ENV_NAME=${ENV_NAME:-appworld}
ENV_RUN_SCRIPT=${ENV_RUN_SCRIPT:-examples/agent_env/appworld/scripts/run_appworld_grpo.sh}
ENV_CONFIG=${ENV_CONFIG:-examples/agent_env/appworld/train_config.yaml}

TOPOLOGY_PROFILE=${TOPOLOGY_PROFILE:-configs/agent_env/topology/2train_4gpu.env}
TRAIN_PROFILE=${TRAIN_PROFILE:-configs/agent_env/train/qwen3_8b_grpo_2x8.env}

MODEL_BASENAME=${MODEL_BASENAME:-Qwen3-8B}
EXP_PROJECT=${EXP_PROJECT:-${PROJECT_NAME:-${MODEL_BASENAME}_appworld_grpo}}
EXP_NAME=${EXP_NAME:-${RUN_NAME:-appworld-${MODEL_BASENAME}-grpo-2x8-gbs128}}
ENV_POOL_SIZE=${ENV_POOL_SIZE:-4}
